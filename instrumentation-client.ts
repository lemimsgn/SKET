// Next.js 15+ expects a project root instrumentation hook module when running in development.
// This file is aliased to the internal "private-next-instrumentation-client" module.
// Keep it intentionally minimal to satisfy the loader and avoid requiring custom instrumentation.

export default function instrumentationClient() {
  return;
}
